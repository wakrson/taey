services:
  taey:
    image: taey
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
      args:
        - USER=${USER}
    environment:
      - DISPLAY=${DISPLAY}
      - USER=${USER}
      - DEBIAN_FRONTEND=noninteractive
    volumes:
      - ./:/home/${USER}/taey
      - /tmp/.X11-unix:/tmp/.X11-unix
    runtime: nvidia
    tty: true
    stdin_open: true
    privileged: true
    network_mode: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]